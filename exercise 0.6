sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes a note and clicks 'Save'

    Note right of browser: JavaScript intercepts the form submission and prevents page reload

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    server-->>browser: JSON response with the new note { "content": "...", "date": "..." }
    deactivate server

    Note right of browser: Browser executes JavaScript callback

    Note right of browser: The new note is added to local notes list

    Note right of browser: Browser re-renders the notes on the page dynamically without reloading
